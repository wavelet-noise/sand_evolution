name: deploy

on:
  push:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: wasm-pack-action
      uses: jetli/wasm-pack-action@v0.3.0
    - name: Build 🛠
      run: wasm-pack build --release --target web sand_evolution_lib
    - name: Archive 💼
      run: |
        echo "- builed files -"
        ls sand_evolution_lib/pkg
        echo "----------------"
        mkdir dest
        mkdir dest/sand_evolution_lib
        mkdir dest/sand_evolution_lib/pkg
        cp -r sand_evolution_lib/pkg dest/sand_evolution_lib/pkg
        echo "-  clone main  -"
        git clone https://github.com/wavelet-noise/sand_evolution.git repo
        ls repo
        echo "----------------"
        cp repo/index.html dest/index.html
        cp repo/favicon.ico dest/favicon.ico
        echo "---  result  ---"
        ls -R dest
    - name: Deploy 🚀
      uses: s0/git-publish-subdir-action@develop
      env:
        REPO: self
        BRANCH: gh-pages
        FOLDER: dest
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
